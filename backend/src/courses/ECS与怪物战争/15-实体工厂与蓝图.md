# 15 实体工厂与蓝图

<div class="video-container">
  <div id="bilibili" class="video-content">
    <!-- B站嵌入：使用 https 明确协议（避免 file:// 或 http 导致被浏览器拦截） -->
    <iframe
      class="video-frame"
      src="https://player.bilibili.com/player.html?bvid=BV1c5CYBNE5M&page=1&autoplay=0&danmaku=0&high_quality=1"
      width="100%"
      height="480"
      scrolling="no"
      frameborder="0"
      allowfullscreen>
    </iframe>
  </div>
</div>

[在 Bilibili 上观看](https://www.bilibili.com/video/BV1c5CYBNE5M)

## 📖 概述

上一节我们已经能从 Tiled 解析出路径节点，并让敌人沿着路径移动，但当时敌人是“手写测试实体”：

- `registry.create()`
- `emplace<Transform/Velocity/Enemy/Sprite/Render...>()`

这种写法能跑通闭环，但有两个痛点会很快出现：

1) **创建逻辑分散**：不同敌人/不同单位会把 `GameScene` 塞满 `emplace`，很难维护。  
2) **调参不方便**：HP/ATK/速度/动画/音效都在代码里，改一次就要重新编译。  

这一节我们引入两套“数据驱动”的基础设施：

- **BlueprintManager（蓝图管理器）**：从 JSON 加载并保存“单位蓝图（纯数据）”
- **EntityFactory（实体工厂）**：拿蓝图来创建实体，并按步骤把组件加齐

![](../本期参考/PPT截图/怪物战争.063.png)

> PPT 第 63 页：第 15 节标题页

本节对应代码标签：`15-实体工厂与蓝图`（基线：`14-设定移动路径`）。

## 🎯 学习目标

- 理解“蓝图（Blueprint）”在 ECS 项目里的定位：**纯数据、可配置、可复用**
- 设计蓝图结构体（`entity_blueprint.h`），并从 `enemy_data.json` 解析出敌人蓝图
- 实现 `BlueprintManager`：加载 JSON → 解析子蓝图 → 存到 map（用 `entt::hashed_string` 做 key）
- 实现 `EntityFactory::createEnemyUnit()`：通过蓝图创建敌人实体，并补齐组件与标签
- 在 `GameScene` 中接入蓝图与工厂：从“手写 emplace”切到“按蓝图生成”

## 🧱 1) 架构：蓝图管理器 + 实体工厂（数据与逻辑分离）

这节的结构图非常清晰：

![](../本期参考/PPT截图/怪物战争.064.png)

> PPT 第 64 页：EntityFactory 负责“怎么创建实体”（逻辑），BlueprintManager 负责“有哪些蓝图数据”（数据）

核心原则是：

- **BlueprintManager** 只负责“把 JSON 变成结构体并存起来”
- **EntityFactory** 只负责“按蓝图把组件加到 entity 上”

这样将来要新增单位类型/改数值/换动画，只需要改 `assets/data/*.json`，不需要改游戏逻辑代码。

## 🗂️ 2) 蓝图数据：`assets/data/enemy_data.json`

这一节新增了敌人蓝图配置文件：`assets/data/enemy_data.json`。

它的顶层结构是一个对象：key 是“敌人类型名”，value 是该类型的配置数据。比如 `wolf`：

```json
{
  "wolf": {
    "name": "狼",
    "hp": 300,
    "atk": 100,
    "def": 30,
    "range": 20,
    "atk_interval": 1.5,
    "speed": 80,
    "ranged": false,
    "sprite_sheet": "assets/textures/Enemy/wolf.png",
    "face_right": false,
    "width": 192,
    "height": 192,
    "offset_x": -96,
    "offset_y": -148,
    "animation": {
      "idle": { "duration": 50, "row": 0, "frames": [0,1,2] },
      "walk": { "duration": 25, "row": 1, "frames": [0,1,2] }
    }
  }
}
```

你可以把它理解为：“把敌人的所有关键参数都写在 JSON 里”，大体分成几类：

- **属性（stats）**：`hp/atk/def/range/atk_interval`
- **移动与类型**：`speed`、`ranged`
- **显示信息**：`name`（可选 `description`）
- **精灵与动画**：`sprite_sheet/width/height/offset/animation{...}`
- **音效（可选）**：`sounds{ emit: ... }`

> 文件里还出现了 `projectile` 之类字段（例如远程单位），本节先不解析，留给后续战斗系统扩展。

## 🧩 3) 蓝图结构体：用 C++ 结构体承载 JSON（纯数据）

为了让“解析 → 使用”更清晰，本节把蓝图拆成多层结构体（每个都只负责一小块数据）：

```cpp
// src/game/data/entity_blueprint.h（节选）
struct StatsBlueprint { float hp_, atk_, def_, range_, atk_interval_; };
struct SpriteBlueprint {
    entt::id_type id_{entt::null};
    std::string path_;
    engine::utils::Rect src_rect_{};
    glm::vec2 size_{};
    glm::vec2 offset_{};
    bool face_right_{true};
};
struct AnimationBlueprint { float ms_per_frame_{}; int row_{}; std::vector<int> frames_; };
struct SoundBlueprint { std::unordered_map<entt::id_type, entt::id_type> sounds_; };
struct EnemyBlueprint { bool ranged_{false}; float speed_{}; };
struct DisplayInfoBlueprint { std::string name_; std::string description_; };

struct EnemyClassBlueprint {
    entt::id_type class_id_{entt::null};
    std::string class_name_;
    StatsBlueprint stats_{};
    EnemyBlueprint enemy_{};
    SoundBlueprint sounds_{};
    SpriteBlueprint sprite_{};
    DisplayInfoBlueprint display_info_{};
    std::unordered_map<entt::id_type, AnimationBlueprint> animations_;
};
```

注意这里的命名和上一节我们总结的 ECS 思想是一致的：**蓝图就是纯数据**，本质上类似“组件的配置版”。

## 📚 4) BlueprintManager：加载 JSON，解析并缓存蓝图

`BlueprintManager` 负责读取 JSON 文件，并把每个敌人类型解析为 `EnemyClassBlueprint`，存入一个 `unordered_map`：

```cpp
// src/game/factory/blueprint_manager.h（节选）
std::unordered_map<entt::id_type, data::EnemyClassBlueprint> enemy_class_blueprints_;

[[nodiscard]] bool loadEnemyClassBlueprints(std::string_view enemy_json_path);
const data::EnemyClassBlueprint& getEnemyClassBlueprint(entt::id_type id) const;
```

### 4.1 解析入口：loadEnemyClassBlueprints

它会遍历 JSON 顶层的每一项：

- `class_name`：如 `"wolf"`
- `class_id`：用 `entt::hashed_string` 把字符串变成稳定的整数 key

```cpp
// src/game/factory/blueprint_manager.cpp（节选）
for (auto& [class_name, data_json] : json.items()) {
    entt::id_type class_id = entt::hashed_string(class_name.c_str());
    data::StatsBlueprint stats = parseStats(data_json);
    data::SpriteBlueprint sprite = parseSprite(data_json);
    auto animations = parseAnimationsMap(data_json);
    data::SoundBlueprint sounds = parseSound(data_json);
    data::EnemyBlueprint enemy = parseEnemy(data_json);
    data::DisplayInfoBlueprint display_info = parseDisplayInfo(data_json);
    enemy_class_blueprints_.emplace(class_id, data::EnemyClassBlueprint{class_id, class_name, ...});
}
```

### 4.2 解析拆分：每个子蓝图一个 parseXXX

这节写作上值得学的一点：把解析拆分成多个小函数，避免一个函数塞满字段。

例如 `parseStats`：

```cpp
data::StatsBlueprint BlueprintManager::parseStats(const nlohmann::json& json) {
    return data::StatsBlueprint{
        json["hp"].get<float>(),
        json["atk"].get<float>(),
        json["def"].get<float>(),
        json["range"].get<float>(),
        json["atk_interval"].get<float>()
    };
}
```

`parseAnimationsMap` 会遍历 `animation` 的每一项，把动画名也哈希成 id（方便以后用 `_hs` 查找）：

```cpp
for (auto& [anim_name, anim_data] : json["animation"].items()) {
    auto anim_name_id = entt::hashed_string(anim_name.c_str());
    std::vector<int> frames = anim_data["frames"].get<std::vector<int>>();
    animations.emplace(anim_name_id, data::AnimationBlueprint{...});
}
```

`parseSound` 的一个小细节是：它在“加载蓝图”时就顺手让 `ResourceManager` 预加载音效：

```cpp
for (auto& [sound_key, sound_value] : json["sounds"].items()) {
    std::string sound_path = sound_value.get<std::string>();
    entt::id_type sound_id = entt::hashed_string(sound_path.c_str());
    resource_manager_.loadSound(sound_id, sound_path);
    sounds.sounds_.emplace(entt::hashed_string(sound_key.c_str()), sound_id);
}
```

这样工厂创建实体时只需要把 sound_id 塞进组件即可。

## 🏭 5) EntityFactory：拿蓝图创建实体（按步骤加组件）

`EntityFactory` 的职责只有一个：**创建实体**。

这一节先实现了敌人单位的创建接口：

```cpp
// src/game/factory/entity_factory.h（节选）
entt::entity createEnemyUnit(entt::id_type class_id,
                             const glm::vec2& position,
                             int target_waypoint_id,
                             int level = 1,
                             int rarity = 1);
```

实现上，它会：

1) `registry_.create()` 创建实体  
2) 从 `BlueprintManager` 拿到 `EnemyClassBlueprint`  
3) 按顺序调用 `addXXXComponent(...)` 把组件“装配”上去  

```cpp
// src/game/factory/entity_factory.cpp（节选）
const auto& blueprint = blueprint_manager_.getEnemyClassBlueprint(class_id);
addTransformComponent(entity, position);
addSpriteComponent(entity, blueprint.sprite_);
addAnimationComponent(entity, blueprint.animations_, blueprint.sprite_, "walk"_hs);
addAudioComponent(entity, blueprint.sounds_);
addStatsComponent(entity, blueprint.stats_, level, rarity);
addEnemyComponent(entity, blueprint.enemy_, target_waypoint_id);
registry_.emplace<game::component::ClassNameComponent>(entity, class_id, blueprint.display_info_.name_);
registry_.emplace<engine::component::RenderComponent>(entity); // 默认主图层
```

### 5.1 新增 StatsComponent + 数值缩放（level/rarity）

本节新增了 `StatsComponent`：

```cpp
// src/game/component/stats_component.h（节选）
struct StatsComponent {
    float hp_{}, max_hp_{}, atk_{}, def_{};
    float range_{}, atk_interval_{}, atk_timer_{};
    int level_{1}, rarity_{1};
};
```

并在引擎工具里加入了一个非常“游戏化”的数值缩放函数：

```cpp
// src/engine/utils/math.h（节选）
inline float statModify(float base, int level = 1, int rarity = 1) {
    return base * (0.95f + 0.05f * level) * (0.9f + 0.1f * rarity);
}
```

现在敌人的属性不再是固定值，而是“基础值 × 等级系数 × 稀有度系数”。（系数还很粗糙，但足够演示“数据驱动 + 可调参”。）

### 5.2 方向与类型标签：FaceLeft / Melee / Ranged

本节在 `src/game/defs/tags.h` 里新增了几个标签组件：

- `FaceLeftTag`：某些 sprite 默认朝左（`face_right=false`），可用标签驱动“翻转显示”
- `MeleeUnitTag / RangedUnitTag`：区分近战/远程，为后续攻击系统铺路

工厂会在创建敌人时自动补这些标签：

```cpp
if (!sprite.face_right_) registry_.emplace<game::defs::FaceLeftTag>(entity);
if (enemy.ranged_) registry_.emplace<game::defs::RangedUnitTag>(entity);
else registry_.emplace<game::defs::MeleeUnitTag>(entity);
```

## 🎮 6) GameScene 接入：从“手写测试敌人”切到“工厂批量生成”

`GameScene` 新增了初始化流程 `initEntityFactory()`：

```cpp
// src/game/scene/game_scene.cpp（节选）
blueprint_manager_ = std::make_shared<game::factory::BlueprintManager>(context_.getResourceManager());
blueprint_manager_->loadEnemyClassBlueprints("assets/data/enemy_data.json");
entity_factory_ = std::make_unique<game::factory::EntityFactory>(registry_, *blueprint_manager_);
```

然后测试函数 `createTestEnemy()` 就变得非常干净：只需要指定类型与起点即可：

```cpp
// src/game/scene/game_scene.cpp（节选）
entity_factory_->createEnemyUnit("wolf"_hs, position, start_index);
entity_factory_->createEnemyUnit("slime"_hs, position, start_index);
entity_factory_->createEnemyUnit("goblin"_hs, position, start_index);
entity_factory_->createEnemyUnit("dark_witch"_hs, position, start_index);
```

到这里，“新增一种敌人”的成本就非常低了：

1) 往 `enemy_data.json` 里加一段配置  
2) 在需要生成的地方调用 `createEnemyUnit("new_enemy"_hs, ...)`  

## ✅ 本节小结

- 引入数据驱动的实体生成链路：`BlueprintManager（数据）` + `EntityFactory（逻辑）`
- 用 `enemy_data.json` 定义敌人属性/动画/音效/速度等关键参数，减少硬编码
- `EntityFactory` 负责“装配组件”：Transform/Sprite/Animation/Audio/Stats/Enemy/Render + 多种标签
- `GameScene` 从手写 emplace 迁移为“按蓝图批量生成”，并能轻松扩展敌人种类

## ➡️ 下一节预告

下一节（第 16 节：玩家实体与阻拦敌人）会开始把塔防核心交互搬进来：创建玩家单位，并加入“阻挡系统”，让敌人与玩家发生真正的接触与停滞。

