# 标题场景及场景重叠

<div class="video-container">
  <div id="bilibili" class="video-content">
    <!-- B站嵌入：使用 https 明确协议（避免 file:// 或 http 导致被浏览器拦截） -->
    <iframe
      class="video-frame"
      src="https://player.bilibili.com/player.html?bvid=BV1pGe8zxEsA&page=1&autoplay=0&danmaku=0&high_quality=1"
      width="100%"
      height="480"
      scrolling="no"
      frameborder="0"
      allowfullscreen>
    </iframe>
  </div>
</div>

[在 Bilibili 上观看](https://www.bilibili.com/video/BV1pGe8zxEsA)

## 📌 问题背景

欢迎来到《C++游戏开发之旅》的第34课！到目前为止，我们的"阳光岛"项目一启动就直接进入了游戏关卡。这对于开发和测试来说很方便，但一个完整的游戏需要一个专业的起点——**标题场景（Title Scene）**，也就是我们常说的主菜单。

### ⚠️ 当前的问题

```
无主菜单的游戏：
┌─────────────────────────┐
│ 启动游戏                │
│    ↓                    │
│ 直接进入关卡            │
│    ↓                    │
│ 👤 开始游戏             │
│                         │
│ ❌ 无欢迎界面           │
│ ❌ 无功能菜单           │
│ ❌ 无场景管理           │
│ ❌ 体验不专业           │
└─────────────────────────┘
```

| 问题 | 说明 | 影响 |
|------|------|------|
| **无入口场景** | 直接进入游戏关卡 | 缺少过渡 |
| **无菜单系统** | 没有开始/设置/退出 | 功能不完整 |
| **场景切换单一** | 只能替换场景 | 无法实现弹窗 |
| **不够专业** | 缺少标题界面 | 用户体验差 |

### 💡 解决方案：标题场景与场景堆栈

**核心思想**：
1. 创建TitleScene作为游戏入口
2. 实现动态滚动背景
3. 使用场景堆栈管理
4. 支持场景重叠效果

```
场景系统架构：
┌──────────────────────────┐
│    SceneManager          │ ← 场景管理器
│  ┌────────────────────┐  │
│  │ Scene Stack        │  │ ← 场景堆栈 ✨
│  │ ┌────────────────┐ │  │
│  │ │ HelpsScene     │ │  │   Push/Pop
│  │ ├────────────────┤ │  │   ↑ 重叠
│  │ │ TitleScene     │ │  │   Replace
│  │ └────────────────┘ │  │   ↑ 替换
│  └────────────────────┘  │
└──────────────────────────┘

三种切换方式：
• Replace - 完全替换
• Push    - 压入堆栈
• Pop     - 弹出堆栈
```

### 🎯 本课目标

| 目标 | 说明 |
|------|------|
| **创建TitleScene** | 构建独立的标题场景 |
| **动态背景** | 实现滚动背景效果 |
| **场景堆栈** | 理解Push/Pop机制 |
| **场景重叠** | 实现HelpsScene弹窗 |

---

## 第一部分：游戏入口调整

### 💡 从标题场景开始

首先，我们需要让游戏从新的 `TitleScene` 启动，而不是之前的 `GameScene`。

---

## 1. 调整游戏入口

### src/engine/core/game_app.cpp（更新）

```cpp
// ...
#include "../scene/scene_manager.h"
// #include "../../game/scene/game_scene.h"  // 旧的入口
#include "../../game/scene/title_scene.h"   // 新的入口

// ...

bool GameApp::init() {
    // ... (其他初始化代码不变) ...
    if (!initSceneManager()) return false;

    // 创建第一个场景并压入栈 (现在是 TitleScene)
    auto scene = std::make_unique<game::scene::TitleScene>(*context_, *scene_manager_);
    scene_manager_->requestPushScene(std::move(scene));

    is_running_ = true;
    return true;
}
```

### 入口变化

```
游戏启动流程变化：
┌──────────────────────┐
│ 之前（测试用）：     │
│ GameApp::init()      │
│    ↓                 │
│ GameScene            │
│    ↓                 │
│ 直接开始游戏         │
└──────────────────────┘

┌──────────────────────┐
│ 现在（正式版）：     │
│ GameApp::init()      │
│    ↓                 │
│ TitleScene ✨        │
│    ↓                 │
│ 主菜单界面           │
│    ↓                 │
│ 用户选择开始         │
│    ↓                 │
│ GameScene            │
└──────────────────────┘
```

完成了这一步，整个游戏的流程就正式由我们的新标题场景接管了。

### 流程对比

| 方式 | 入口场景 | 用户体验 | 专业度 |
|-----|---------|---------|-------|
| **直接进入** | GameScene | 简单粗暴 | 低 ❌ |
| **标题场景** | TitleScene | 完整流程 | 高 ✅ |

---

## 第二部分：标题场景设计

### 💡 构建专业的标题界面

TitleScene 是一个完整的场景，它拥有自己的背景、UI和逻辑。我们将它设计成一个**带有动态背景和四个功能按钮**的界面。

---

## 2. TitleScene 设计

### src/game/scene/title_scene.h（新建文件）

```cpp
namespace game::scene {

/**
 * @brief 标题场景类，提供4个按钮：开始游戏、加载游戏、帮助、退出
 */
class TitleScene final : public engine::scene::Scene {
private:
    std::shared_ptr<game::data::SessionData> session_data_;

public:
    /**
     * @brief 构造函数
     * @param context 引擎上下文
     * @param scene_manager 场景管理器
     * @param game_play_state 指向游戏玩法状态的共享指针
     */
    TitleScene(engine::core::Context& context,
               engine::scene::SceneManager& scene_manager,
               std::shared_ptr<game::data::SessionData> session_data = nullptr);

    ~TitleScene() override = default;

    // --- 核心方法 --- //
    void init() override;
    void update(float delta_time) override;

private:
    // 初始化 UI 元素
    void createUI();

    // 按钮回调函数
    void onStartGameClick();
    void onLoadGameClick();
    void onHelpsClick();
    void onQuitClick();
};
```

### 💡 TitleScene 结构

```
TitleScene 组成：
┌──────────────────────────┐
│    TitleScene            │
│  ┌────────────────────┐  │
│  │ 动态背景           │  │ ← Tiled地图
│  │ • level0.tmj       │  │   自动滚动
│  │ • 相机移动         │  │
│  └────────────────────┘  │
│  ┌────────────────────┐  │
│  │ UI界面             │  │ ← 标题 + 按钮
│  │ • 标题图片         │  │
│  │ • 4个按钮          │  │
│  │ • Credits文字      │  │
│  └────────────────────┘  │
│  ┌────────────────────┐  │
│  │ 按钮回调           │  │ ← 4个功能
│  │ • 开始游戏         │  │
│  │ • 加载游戏         │  │
│  │ • 帮助             │  │
│  │ • 退出             │  │
│  └────────────────────┘  │
└──────────────────────────┘
```

### TitleScene 功能列表

| 功能 | 实现方式 | 作用 |
|------|---------|------|
| **动态背景** | 加载地图 + 移动相机 | 视觉效果 |
| **标题显示** | UIImage | 游戏名称 |
| **开始游戏** | UIButton + Replace | 进入关卡 |
| **加载游戏** | UIButton + 存档系统 | 继续游戏 |
| **帮助** | UIButton + Push | 显示帮助 |
| **退出** | UIButton + Quit | 退出游戏 |

---

## 3. 动态背景实现

为了让标题界面不那么单调，我们不使用静态图片作为背景，而是**加载一个真实的Tiled地图（level0.tmj）**，并通过在 `update` 函数中持续移动相机，来实现背景自动向右滚动的视觉效果。

### src/game/scene/title_scene.cpp（实现）

```cpp
// 初始化场景
void TitleScene::init() {
    if (is_initialized_) {
        return;
    }
    // 加载背景地图
    engine::scene::LevelLoader level_loader;
    if (!level_loader.loadLevel("assets/maps/level0.tmj", *this)) {
         spdlog::error("加载背景失败");
         return;
    }

    // 创建 UI 元素
    createUI();

    Scene::init();
    spdlog::trace("TitleScene 初始化完成.");
}

// 更新场景逻辑
void TitleScene::update(float delta_time) {
    Scene::update(delta_time);

    // 相机自动向右移动，创建动态效果
    context_.getCamera().move(glm::vec2(delta_time * 100.0f, 0.0f));
}
```

### 动态背景原理

```
动态背景实现：
┌──────────────────────┐
│ 1. 加载地图          │
│    LevelLoader       │
│    level0.tmj        │
├──────────────────────┤
│ 2. 每帧更新          │
│    update()          │
│    ↓                 │
│    相机右移          │
│    move(100*dt, 0)   │
├──────────────────────┤
│ 3. 视觉效果          │
│    地图向左滚动      │
│    无限循环效果      │
└──────────────────────┘

效果：
┌─────────────────────────┐
│ 🌳🌳🌳 → → →           │
│ ═══════════            │
│   [SUNNY LAND]         │
│                         │
│ [开始] [加载] [帮助] [退出]
└─────────────────────────┘
```

### 滚动速度计算

```cpp
context_.getCamera().move(glm::vec2(delta_time * 100.0f, 0.0f));
```

| 参数 | 值 | 说明 |
|-----|---|------|
| **delta_time** | 帧时间 | 约0.016秒(60fps) |
| **速度** | 100.0f | 像素/秒 |
| **每帧移动** | 100 × 0.016 ≈ 1.6像素 | 平滑滚动 |

---

## 4. UI布局设计

标题场景的UI布局是核心。我们使用了之前课程中构建的 `UIPanel`, `UIImage`, `UIButton` 和 `UILabel` 来组合出整个界面。

### 布局策略

特别值得注意的是按钮的布局方式：

```
按钮布局步骤：
┌──────────────────────┐
│ 1. 定义参数          │
│    • 按钮尺寸        │
│    • 按钮数量        │
│    • 按钮间距        │
├──────────────────────┤
│ 2. 计算面板尺寸      │
│    width = n×w +     │
│            (n-1)×s   │
├──────────────────────┤
│ 3. 水平居中          │
│    x = (screen - w)/2│
├──────────────────────┤
│ 4. 相对坐标添加      │
│    每个按钮相对Panel │
└──────────────────────┘
```

1. 我们先定义了4个按钮的统一尺寸和它们之间的间距
2. 然后计算出容纳这4个按钮需要的 UIPanel 的总尺寸
3. 将这个 UIPanel 在屏幕上水平居中
4. 最后，将4个按钮依次添加到 UIPanel 中，它们的坐标是相对于 UIPanel 的左上角，布局起来非常方便

### src/game/scene/title_scene.cpp（UI创建）

```cpp
void TitleScene::createUI() {
    auto window_size = glm::vec2(640.0f, 360.0f);

    // ... (创建标题图片的代码) ...

    // --- 创建按钮面板并居中 ---
    float button_width = 96.0f;
    float button_height = 32.0f;
    float button_spacing = 20.0f;
    int num_buttons = 4;

    // 计算面板总宽度和位置
    float panel_width = num_buttons * button_width + (num_buttons - 1) * button_spacing;
    float panel_height = button_height;
    float panel_x = (window_size.x - panel_width) / 2.0f;
    float panel_y = window_size.y * 0.65f;

    auto button_panel = std::make_unique<engine::ui::UIPanel>(
        glm::vec2(panel_x, panel_y),
        glm::vec2(panel_width, panel_height)
    );

    // --- 创建按钮并添加到 UIPanel ---
    glm::vec2 current_button_pos = glm::vec2(0.0f, 0.0f);
    glm::vec2 button_size = glm::vec2(button_width, button_height);

    // Start Button
    auto start_button = std::make_unique<engine::ui::UIButton>(context_, 
        "...", "...", "...", current_button_pos, button_size, [this]() { this->onStartGameClick();});
    button_panel->addChild(std::move(start_button));

    // Load Button
    current_button_pos.x += button_width + button_spacing;
    auto load_button = std::make_unique<engine::ui::UIButton>(context_, 
        "...", "...", "...", current_button_pos, button_size, [this]() { this->onLoadGameClick();});
    button_panel->addChild(std::move(load_button));

    // Helps Button
    current_button_pos.x += button_width + button_spacing;
    auto helps_button = std::make_unique<engine::ui::UIButton>(context_, 
        "...", "...", "...", current_button_pos, button_size, [this]() { this->onHelpsClick();});
    button_panel->addChild(std::move(helps_button));
    
    // Quit Button
    current_button_pos.x += button_width + button_spacing;
    auto quit_button = std::make_unique<engine::ui::UIButton>(context_, 
        "...", "...", "...", current_button_pos, button_size, [this]() { this->onQuitClick();});
    button_panel->addChild(std::move(quit_button));

    // 将 UIPanel 添加到UI管理器
    ui_manager_->addElement(std::move(button_panel));

    // ... (创建Credits标签的代码) ...
}
```

### 布局可视化

```
UI布局结构：
┌─────────────────────────┐ 640×360
│                         │
│    [SUNNY LAND]         │ ← 标题图片
│                         │
│  ┌──────────────────┐   │
│  │ Button Panel     │   │ ← 居中的面板
│  │ ┌──┐┌──┐┌──┐┌──┐│   │
│  │ │开││加││帮││退││   │ ← 4个按钮
│  │ │始││载││助││出││   │   相对坐标
│  │ └──┘└──┘└──┘└──┘│   │
│  └──────────────────┘   │
│                         │
│  Credits: xxx           │ ← 版权信息
└─────────────────────────┘

面板宽度计算：
4×96 + 3×20 = 444像素
居中位置：(640-444)/2 = 98像素
```

### 布局计算表

| 项目 | 计算公式 | 结果 |
|-----|---------|------|
| **按钮宽度** | 固定 | 96px |
| **按钮高度** | 固定 | 32px |
| **间距** | 固定 | 20px |
| **面板宽度** | 4×96 + 3×20 | 444px |
| **面板X坐标** | (640-444)/2 | 98px |
| **面板Y坐标** | 360×0.65 | 234px |

### 相对坐标优势

```
相对坐标系统优势：
┌──────────────────────┐
│ UIPanel (98, 234)    │ ← 绝对坐标
│  ┌────────────────┐  │
│  │ Button1 (0,0)  │  │ ← 相对Panel
│  ├────────────────┤  │
│  │ Button2 (116,0)│  │ ← 相对Panel
│  ├────────────────┤  │
│  │ Button3 (232,0)│  │ ← 相对Panel
│  ├────────────────┤  │
│  │ Button4 (348,0)│  │ ← 相对Panel
│  └────────────────┘  │
└──────────────────────┘

优势：
✅ 移动Panel，按钮自动跟随
✅ 布局计算简单
✅ 易于调整和维护
```

---

## 第三部分：场景切换机制

### 💡 三种场景切换方式

现在到了最关键的部分：**场景切换**。SceneManager 内部维护着一个场景的堆栈。我们通过不同的请求来操作这个堆栈。

---

## 5. 场景切换：Replace vs. Push/Pop

### 三种切换机制

```
场景堆栈操作：
┌──────────────────────────┐
│  SceneManager            │
│  ┌────────────────────┐  │
│  │  Scene Stack       │  │
│  │                    │  │
│  │  ┌──────────────┐  │  │
│  │  │              │  │  │
│  │  │              │  │  │
│  │  └──────────────┘  │  │
│  └────────────────────┘  │
└──────────────────────────┘

三种操作：
1. Replace - 替换所有
2. Push    - 压入新场景
3. Pop     - 弹出顶层
```

### 1. requestReplaceScene（替换）

**替换**。这个操作会清空整个场景堆栈，然后将新场景作为唯一的元素放进去。

```
Replace 操作：
┌──────────────┐
│ Stack Before │
│ ┌──────────┐ │
│ │TitleScene│ │ ← 当前场景
│ └──────────┘ │
└──────────────┘
       ↓ Replace
┌──────────────┐
│ Stack After  │
│ ┌──────────┐ │
│ │GameScene │ │ ← 新场景
│ └──────────┘ │
└──────────────┘

特点：
• 清空整个堆栈
• 不可逆的切换
• 重大流程转换
```

**适用于**：重大的、不可逆的流程切换，比如从"主菜单"进入"游戏关卡"。

### 2. requestPushScene（压入/重叠）

**压入/重叠**。这个操作会暂停当前栈顶的场景（但不会销毁它），然后将一个新场景压入栈顶，使其成为新的活动场景。

```
Push 操作：
┌──────────────┐
│ Stack Before │
│ ┌──────────┐ │
│ │TitleScene│ │ ← 当前场景
│ └──────────┘ │
└──────────────┘
       ↓ Push
┌──────────────┐
│ Stack After  │
│ ┌──────────┐ │
│ │HelpsScene│ │ ← 新场景（活动）
│ ├──────────┤ │
│ │TitleScene│ │ ← 暂停（保留）
│ └──────────┘ │
└──────────────┘

特点：
• 保留原场景
• 可逆的切换
• 临时遮挡界面
```

**适用于**：需要临时遮挡当前场景的界面，比如"暂停菜单"或"帮助界面"。

### 3. requestPopScene（弹出）

**弹出**。这个操作会销毁栈顶的场景，并恢复它下面那个场景的运行。

```
Pop 操作：
┌──────────────┐
│ Stack Before │
│ ┌──────────┐ │
│ │HelpsScene│ │ ← 当前场景
│ ├──────────┤ │
│ │TitleScene│ │ ← 暂停中
│ └──────────┘ │
└──────────────┘
       ↓ Pop
┌──────────────┐
│ Stack After  │
│ ┌──────────┐ │
│ │TitleScene│ │ ← 恢复运行
│ └──────────┘ │
└──────────────┘

特点：
• 销毁顶层场景
• 恢复下层场景
• 关闭临时界面
```

它通常与 Push 成对出现，用于关闭临时界面。

### 三种机制对比表

| 操作 | 堆栈变化 | 原场景 | 新场景 | 适用场景 |
|-----|---------|-------|-------|---------|
| **Replace** | 清空→压入 | 销毁 | 唯一场景 | 主菜单→游戏 |
| **Push** | 压入 | 暂停保留 | 压入栈顶 | 显示帮助/暂停 |
| **Pop** | 弹出 | 恢复运行 | 销毁 | 关闭弹窗 |

---

## 6. 在TitleScene中的应用

在 TitleScene 中，我们完美地演示了这两种机制：

### src/game/scene/title_scene.cpp（按钮回调）

```cpp
// "开始游戏"按钮：用GameScene替换掉当前所有场景
void TitleScene::onStartGameClick() {
    spdlog::debug("开始游戏按钮被点击。");
    if (session_data_) {
        session_data_->reset();
    }
    scene_manager_.requestReplaceScene(std::make_unique<GameScene>(context_, scene_manager_, session_data_));
}

// "帮助"按钮：在TitleScene之上压入一个HelpsScene
void TitleScene::onHelpsClick() {
    spdlog::debug("帮助按钮被点击。");
    scene_manager_.requestPushScene(std::make_unique<HelpsScene>(context_, scene_manager_));
}

// "退出"按钮：直接设置退出标志
void TitleScene::onQuitClick() {
    spdlog::debug("退出按钮被点击。");
    context_.getInputManager().setShouldQuit(true);
}
```

### 回调功能说明

```
按钮功能实现：
┌──────────────────────┐
│ 1. 开始游戏          │
│    Replace           │
│    TitleScene        │
│       ↓              │
│    GameScene         │
│    • 重置数据        │
│    • 完全替换        │
├──────────────────────┤
│ 2. 加载游戏          │
│    Replace           │
│    • 加载存档        │
│    • 进入关卡        │
├──────────────────────┤
│ 3. 帮助              │
│    Push              │
│    TitleScene        │
│    HelpsScene ← 叠加 │
│    • 显示说明        │
│    • 可返回          │
├──────────────────────┤
│ 4. 退出              │
│    setShouldQuit()   │
│    • 退出游戏        │
└──────────────────────┘
```

### 场景流转可视化

```
用户操作流程：
    启动游戏
       ↓
┌─────────────┐
│ TitleScene  │ ← 主菜单
└──┬──┬───┬──┘
   │  │   │
   ↓  ↓   ↓
┌────┐ ┌─────────┐
│退出│ │HelpsScene│ ← Push帮助
└────┘ └────┬────┘
            │ 点击返回
            ↓ Pop
       ┌─────────────┐
       │ TitleScene  │ ← 恢复主菜单
       └──────┬──────┘
              │ 点击开始
              ↓ Replace
       ┌─────────────┐
       │  GameScene  │ ← 开始游戏
       └─────────────┘
```

---

## 第四部分：帮助场景实现

### 💡 场景重叠的完美示例

HelpsScene 是 Push/Pop 机制的一个绝佳范例。它的逻辑极其简单。

---

## 7. 实现帮助场景（HelpsScene）

### src/game/scene/helps_scene.h（新建文件）

```cpp
namespace game::scene {

/**
 * @brief 显示帮助信息的场景。
 *
 * 显示一张帮助图片，按鼠标左键退出。
 */
class HelpsScene final : public engine::scene::Scene {
public:
    /**
     * @brief 构造函数。
     * @param context 引擎上下文引用。
     * @param scene_manager 场景管理器引用。
     */
    HelpsScene(engine::core::Context& context, engine::scene::SceneManager& scene_manager);

    ~HelpsScene() override = default;

    // --- 核心方法 ---
    void init() override;
    void handleInput() override;

};

} // namespace game::scene
```

### HelpsScene 设计思路

```
HelpsScene 逻辑：
┌──────────────────────┐
│ 1. 初始化 init()     │
│    • 创建帮助图片    │
│    • 覆盖全屏        │
├──────────────────────┤
│ 2. 输入处理          │
│    handleInput()     │
│    • 检测鼠标点击    │
│    • 请求Pop自己     │
├──────────────────────┤
│ 3. 场景恢复          │
│    Pop后            │
│    • 销毁HelpsScene │
│    • 恢复TitleScene │
└──────────────────────┘

极简设计！
```

### src/game/scene/helps_scene.cpp（实现）

```cpp
#include "helps_scene.h"
#include "../../engine/core/context.h"
#include "../../engine/input/input_manager.h"
#include "../../engine/scene/scene_manager.h"
#include "../../engine/ui/ui_manager.h"
#include "../../engine/ui/ui_image.h"
// ...

void HelpsScene::init() {
    // ...
    // 创建帮助图片 UIImage （让它覆盖整个屏幕）
    auto help_image = std::make_unique<engine::ui::UIImage>(
        "assets/textures/UI/instructions.png",
        glm::vec2(0.0f, 0.0f),
        glm::vec2(640.0f, 360.0f)
    );
    ui_manager_->addElement(std::move(help_image));
    // ...
}

void HelpsScene::handleInput() {
    if (!is_initialized_) return;

    // 检测是否按下鼠标左键，如果是，则弹出当前场景
    if (context_.getInputManager().isActionPressed("MouseLeftClick")) {
        spdlog::debug("鼠标左键被按下, 退出 HelpsScene.");
        scene_manager_.requestPopScene();
    }
}
```

### HelpsScene 工作流程

```
HelpsScene 完整流程：
    TitleScene运行
         ↓
    用户点击"帮助"
         ↓
    Push HelpsScene
         ↓
┌─────────────────────┐
│  HelpsScene显示     │
│  ┌───────────────┐  │
│  │ 帮助图片      │  │
│  │ 操作说明      │  │
│  │               │  │
│  │ [点击返回]    │  │
│  └───────────────┘  │
│  TitleScene暂停     │ ← 在下层
└─────────────────────┘
         ↓
    用户点击鼠标
         ↓
    requestPopScene()
         ↓
    销毁HelpsScene
         ↓
    TitleScene恢复运行
```

当 HelpsScene 被弹出后，场景堆栈下面的 TitleScene 会自动恢复运行，实现了完美的**"关闭帮助，返回主菜单"**的效果。

### Push/Pop 配合使用

```
Push/Pop 成对使用：
┌──────────────────────┐
│ Push时：             │
│ ┌────────────────┐   │
│ │ HelpsScene     │   │ ← 新场景（活动）
│ ├────────────────┤   │
│ │ TitleScene     │   │ ← 暂停
│ └────────────────┘   │
└──────────────────────┘
         ↓ 用户点击
┌──────────────────────┐
│ Pop时：              │
│ ┌────────────────┐   │
│ │ TitleScene     │   │ ← 恢复（活动）
│ └────────────────┘   │
└──────────────────────┘

特点：
✅ 不销毁原场景
✅ 快速恢复
✅ 保留原状态
```

---

## 第五部分：代码优化

### 💡 细节优化与问题修复

最后，我们进行了一些小的代码清理工作。

---

## 8. 代码清理

### 优化内容

1. **移除测试代码**

移除了 GameScene 中用于测试的按钮，因为现在我们有了功能完备的 TitleScene。

2. **重置相机位置**

在 `GameScene::initLevel` 中，添加了一行代码来重置相机位置。

### src/game/scene/game_scene.cpp（优化）

```cpp
bool GameScene::initLevel()
{
    // ...
    // 设置相机边界
    auto world_size = main_layer->getComponent<engine::component::TileLayerComponent>()->getWorldSize();
    context_.getCamera().setLimitBounds(engine::utils::Rect(glm::vec2(0.0f), world_size));
    // 开始时重置相机位置，以免切换场景时晃动
    context_.getCamera().setPosition(glm::vec2(0.0f));     
    // ...
}
```

### 为什么要重置相机？

```
场景切换问题：
┌──────────────────────┐
│ TitleScene           │
│ • 相机持续右移       │
│ • 位置变为(1000, 0)  │
└──────────────────────┘
         ↓ Replace
┌──────────────────────┐
│ GameScene            │
│ • 相机位置(1000, 0)  │
│ • 玩家在(0, 0)       │
│ ❌ 看不到玩家！      │
└──────────────────────┘

解决方案：
┌──────────────────────┐
│ GameScene::initLevel │
│ setPosition(0, 0) ✅ │
│ • 重置相机位置       │
│ • 正确显示玩家       │
└──────────────────────┘
```

这是一个好习惯，可以防止从 TitleScene（相机在移动）切换到 GameScene 时，相机位置不正确导致画面"跳一下"的问题。

### 优化对比

| 问题 | 原因 | 解决方案 | 效果 |
|-----|------|---------|------|
| **画面跳动** | 相机位置未重置 | setPosition(0,0) | 平滑切换 ✅ |
| **测试按钮** | 调试用临时代码 | 移除 | 代码整洁 ✅ |

---

## ✅ 运行测试

编译并运行游戏：

```
游戏完整流程：
┌─────────────────────────┐
│  1. 启动游戏            │
│     ↓                   │
│  2. TitleScene显示      │
│     • 滚动背景          │
│     • 4个按钮           │
│     ↓                   │
│  3. 点击"帮助"          │
│     Push HelpsScene     │
│     • 显示帮助图片      │
│     ↓                   │
│  4. 点击返回            │
│     Pop HelpsScene      │
│     • 回到主菜单        │
│     ↓                   │
│  5. 点击"开始游戏"      │
│     Replace GameScene   │
│     • 进入游戏关卡      │
└─────────────────────────┘

体验完整流程！
```

---

## 🎯 系统架构总结

### 完整的场景系统架构

```
场景系统完整架构：
┌──────────────────────────┐
│      GameApp             │ ← 应用层
│  ┌────────────────────┐  │
│  │ SceneManager       │  │ ← 管理层
│  │ ┌────────────────┐ │  │
│  │ │ Scene Stack    │ │  │ ← 堆栈 ✨
│  │ │                │ │  │
│  │ │ ┌────────────┐ │ │  │
│  │ │ │HelpsScene  │ │ │  │   Pop
│  │ │ ├────────────┤ │ │  │   ↑
│  │ │ │TitleScene  │ │ │  │   Push
│  │ │ └────────────┘ │ │  │   ↑
│  │ └────────────────┘ │  │   Replace
│  └────────────────────┘  │   ↑
└──────────────────────────┘
         ↑
    三种切换方式
```

### 核心成就

我们成功地：

1. ✅ **创建了TitleScene**：专业的标题场景
2. ✅ **实现了动态背景**：地图滚动效果
3. ✅ **掌握了场景堆栈**：三种切换机制
4. ✅ **实现了场景重叠**：Push/Pop帮助界面
5. ✅ **优化了场景切换**：相机位置重置
6. ✅ **完善了游戏流程**：完整的入口体验

### 三种场景切换总结

```
场景切换机制：
┌──────────────────────┐
│ 1. Replace（替换）   │
│    • 清空堆栈        │
│    • 不可逆          │
│    • 主要流程转换    │
│    例：主菜单→游戏   │
├──────────────────────┤
│ 2. Push（压入）      │
│    • 暂停当前        │
│    • 叠加新场景      │
│    • 可逆操作        │
│    例：显示帮助      │
├──────────────────────┤
│ 3. Pop（弹出）       │
│    • 销毁顶层        │
│    • 恢复下层        │
│    • 关闭临时界面    │
│    例：关闭帮助      │
└──────────────────────┘
```

### 应用场景对比

| 场景 | 使用机制 | 原因 | 效果 |
|-----|---------|------|------|
| **开始游戏** | Replace | 主要流程切换 | 进入游戏 |
| **暂停菜单** | Push | 临时遮挡 | 可恢复 |
| **帮助界面** | Push | 临时显示 | 可关闭 |
| **设置界面** | Push | 临时配置 | 可返回 |
| **结束场景** | Replace | 流程结束 | 显示统计 |

### 设计模式应用

```
本课涉及的设计模式：
┌──────────────────────┐
│ 1. 堆栈模式          │
│    场景堆栈管理      │
│    LIFO结构          │
├──────────────────────┤
│ 2. 状态模式          │
│    场景状态切换      │
│    暂停/运行         │
├──────────────────────┤
│ 3. 观察者模式        │
│    场景事件通知      │
│    回调机制          │
├──────────────────────┤
│ 4. 单例模式          │
│    SceneManager      │
│    全局管理          │
└──────────────────────┘
```

---

## 📚 总结

恭喜你！在这一课中，我们为"阳光岛"添加了一个**专业且美观的标题场景**，让它看起来更像一个完整的游戏了。

**关键要点：**
- 🎮 **TitleScene**：专业的游戏入口
- 🌊 **动态背景**：地图滚动效果
- 📚 **场景堆栈**：强大的管理机制
- 🔄 **Replace机制**：主要流程切换
- 📤 **Push/Pop机制**：场景重叠效果
- 🎯 **HelpsScene**：完美的示例
- 🔧 **细节优化**：相机位置重置

> 💡 **设计哲学**：在本节课中，我们深入理解了 SceneManager 强大的堆栈管理机制。理解 Replace 和 Push/Pop 之间的区别，是构建复杂游戏流程（如暂停菜单、设置界面、游戏结束统计等）的关键。Replace 用于不可逆的主要流程切换，而 Push/Pop 则用于临时界面的显示和关闭。这种设计让场景管理变得灵活而强大，能够轻松实现各种复杂的UI流程。

**场景系统的价值**：

```
完整的场景系统：
┌─────────────────────┐
│ 简单切换（60分）：   │
│ • 只能替换场景      │
│ • 无法重叠          │
│ • 功能受限          │
│ → 实现困难          │
├─────────────────────┤
│ 堆栈系统（100分）：  │
│ + Replace替换       │
│ + Push/Pop重叠      │
│ + 灵活管理          │
│ + 完整流程          │
│ → 专业设计！        │
└─────────────────────┘

堆栈让场景"有层次"！
```

---

## 🚀 下一步展望

我们已经有了完整的场景管理系统，但游戏流程还可以更丰富。

- ❓ 如何实现暂停菜单？
- ❓ 如何保存和加载游戏？
- ❓ 如何实现设置界面？
- ❓ 如何显示游戏结束？

在接下来的课程中，我们将继续完善游戏流程，利用今天学到的知识构建更多的游戏场景。

准备好让你的游戏更加完整吧，我们下节课见！🎮🌟