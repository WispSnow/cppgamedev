# 暂未发布的内容

`collaborate.md` 是暂时下线的合作开发页面原稿，保留以便重新整理后恢复。此目录不会进入前端构建或被网站公开提供。

恢复时，将确认后的正文放回 `frontend/public/content/collaborate.md`，恢复页面组件及 `/collaborate` 路由，再按最终内容决定是否恢复页脚、信息页侧栏、作品页、FAQ、关于/联系页的入口，并加入 `scripts/generate-sitemap.js`。上线前重新构建并部署。
